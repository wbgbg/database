angular.module('database')
    .factory('adminUserService',['$rootScope', '$timeout', function($rootScope, $timeout) {
        var that = this;
        var service = {
           
        };
        
        return service;
    }]);